import 'package:flutter/material.dart';

class GalleryScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // Build the gallery screen UI here
    return Center(
      child: Text('Gallery Screen'),
    );
  }
}
